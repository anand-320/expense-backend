pipeline {
    agent {
      node {
        label 'ci-server'
      }
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
}